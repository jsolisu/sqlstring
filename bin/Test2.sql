SELECT
  PFS.PFS_ID ID,
  COALESCE(TKN.PFT_TOKEN, '') TOKEN
FROM SEGURIDAD.SEG_PERFILES PFS
JOIN SEGURIDAD.SEG_PERFIL_TOKENS TKN ON TKN.PFS_ID = PFS.PFS_ID
WHERE
  ROWNUM = 1
  AND (PFS.PFS_PERFIL_ALTERNO = :ID_PERFIL
  OR PFS.PFS_PERFIL = :ID_PERFIL)